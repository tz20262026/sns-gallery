name: Keep Alive

on:
  schedule:
    - cron: '*/5 * * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping SNS Gallery
        run: curl -s https://sns-gallery.onrender.com/api/images?limit=1 > /dev/null
